spring:
  application:
    name: smbms-bill-consumer
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848

server:
  port: 8083

management:
  endpoints:
    web:
      exposure:
        include: "*"